name: Deploy FreelanceOS

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm install

      # Optional (kept from your version)
      - name: Fix permissions
        run: chmod +x node_modules/.bin/vite

      # ---- SAFE ENV CHECKS ----

      - name: Check Supabase env is present (safe)
        run: |
          if [ -z "${VITE_SUPABASE_URL}" ]; then
            echo "VITE_SUPABASE_URL is MISSING"
            exit 1
          else
            echo "VITE_SUPABASE_URL is PRESENT"
          fi

          if [ -z "${VITE_SUPABASE_ANON_KEY}" ]; then
            echo "VITE_SUPABASE_ANON_KEY is MISSING"
            exit 1
          else
            echo "VITE_SUPABASE_ANON_KEY is PRESENT"
          fi
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Check Gemini key is present (safe)
        run: |
          if [ -z "${VITE_GEMINI_API_KEY}" ]; then
            echo "VITE_GEMINI_API_KEY is MISSING"
            exit 1
          else
            echo "VITE_GEMINI_API_KEY is PRESENT"
          fi
        env:
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}

      # ---- BUILD ----

      - name: Build
        run: npm run build
        env:
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      # ---- DEPLOY ----

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
